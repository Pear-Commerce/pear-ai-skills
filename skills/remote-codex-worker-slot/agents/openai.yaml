interface:
  display_name: "Remote Codex Worker Slot"
  short_description: "Claim and drain S3-backed remote Codex jobs"
  default_prompt: "Use $remote-codex-worker-slot to run one bounded queue-draining worker wake cycle."
