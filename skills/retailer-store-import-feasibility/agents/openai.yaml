interface:
  display_name: "Retailer Store Import Feasibility"
  short_description: "Probe store imports and preserve Store.SStore artifacts"
  default_prompt: "Use $retailer-store-import-feasibility to discover, verify, and preserve retailer store import paths, including Store.SStore JSON artifacts and EXTRACTION.md rerun notes when a full store list is produced."
